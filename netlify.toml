[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# o painel é servido como index.html na raiz
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
